export const options = ["rock", "paper", "scissors"];
export const outcomes = ["win", "draw", "lose"];

// Options enum
export const Options = {
  Rock: options[0],
  Paper: options[1],
  Scissors: options[2],
};

// Outcomes enum
export const Outcomes = {
  Win: outcomes[0],
  Draw: outcomes[1],
  Lose: outcomes[2],
};
